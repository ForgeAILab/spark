# {{appName}} — Visual Design

> The durable, product-wide visual language. Every UI task reads from here so the
> product looks like one product. `/start` drafts this before user-facing work;
> refine it as the product matures.
>
> This is the *product-level* design doc. A single change's **technical** "how"
> lives in that change's own `design.md` (`changes/<id>/design.md`) — same name,
> different folder, different job.

## Vibe & reference

<One sentence on the feeling (e.g. "calm, focused, document-like") and one or two
reference products whose look you're aiming near.>

## Color

<Primary, neutral/background, and accent directions; light/dark intent. Keep it to
a small palette and name the role, not just the hex.>

## Typography

<Display vs body typeface direction, type scale, weight usage.>

## Layout & spacing

<Density, max content width, grid/columns, corner radius, elevation conventions.>

## Components

<Conventions shared across the app: buttons, inputs, cards, nav. Prefer one set.
When a UI pack is installed, defer to its primitives and record that choice here.>

## States

- **Empty:** <what a zero-data screen shows and how it invites the first action.>
- **Loading:** <skeletons vs spinners, and where each is used.>
- **Error:** <tone and the recovery affordance offered.>
