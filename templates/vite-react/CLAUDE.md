# CLAUDE.md

Follow `AGENTS.md` for the operating contract. Plan and track work in the `docs/spark/` spec workspace — `docs/spark/changes/<id>/tasks.md` is the task source of truth. Use the `spark` CLI when adding packs or checking installed capabilities.
