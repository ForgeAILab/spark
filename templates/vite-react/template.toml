name = "vite-react"
status = "stable"
provides = ["react", "static", "edge-runtime", "server"]
description = "Vite + React 19 + TypeScript scaffold with a Hono dev API entrypoint; deployable to Cloudflare Workers."
