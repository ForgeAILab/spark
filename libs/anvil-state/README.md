# @forgeailab/anvil-state

Typed read/write helpers for `.anvil/state.json`.

This package lives in `libs/` rather than `packages/` because consumers import it as reusable runtime/workflow code; it is not internal CLI plumbing.
