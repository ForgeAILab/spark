Shared Better Auth runtime helpers for Anvil workspace apps.

## Why libs/ vs packages/?

This lives in `libs/` because it is source-shared workspace runtime code extracted from a reference app; `packages/` is reserved for packaged distribution surfaces.
